<?php namespace Redaxscript\Template; ?>

<!-- main -->

<main id="main" class="rs-main">

	<!-- teaser -->

	<?php if (!Tag::getRegistry('adminParameter')): ?>
		<?php echo Tag::extraRaw()
			->where(
			[
				'alias' => 'teaser',
				'category' => Tag::getRegistry('categoryId')
			])
			->findOne()
			->text; ?>
	<?php endif; ?>

	<!-- error -->

	<?php if (Tag::getRegistry('contentError')): ?>
		<div class="rs-wrapper rs-wrapper-page">
			<?php echo Tag::partial('templates/default/error.phtml'); ?>
		</div>

	<!-- else content and sidebar -->

	<?php else: ?>
		<div class="rs-wrapper rs-wrapper-page rs-has-sidebar">
			<?php echo Tag::partial(
			[
				'templates/default/content.phtml',
				'templates/default/sidebar.phtml'
			]); ?>
		</div>
	<?php endif; ?>

</main>